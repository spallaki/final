this.state = {
  prescriptions: []
}

componentDidMount() {
  getallRx
  this.setState
}

render() {
  thisstate.prescriptions.map( ONEPILL => {
  <TO>
    onPress={ () => this.props.navigation.navigate('All pills page', {Pill: ONEPILL/pillId})}
  </TO>)
}

Pill = this.props.navigation.state.params.Pill.name/id/etc

// Input field for rx number
// Rx number (will populate from individual page, not populate from settings page)
// Geo Location
// Submit
